# Server Configuration
server.port=8080

# Database Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/bitesharing?createDatabaseIfNotExist=true&useSSL=false&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=S@ngl!#10@
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true

# File Upload Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# JWT Configuration
jwt.secret=YourSecretKeyForJWTTokenGeneration12345678901234567890
jwt.expiration=86400000

# Google Maps API
google.maps.api.key=YOUR_GOOGLE_MAPS_API_KEY

# SMS API Configuration (Twilio or similar)
sms.api.key=YOUR_SMS_API_KEY
sms.api.secret=YOUR_SMS_API_SECRET

# CORS Configuration
cors.allowed.origins=http://localhost:3000

# File Storage
file.upload.dir=./uploads

